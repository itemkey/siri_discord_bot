services:
  discord-bot:
    build: .
    container_name: siri-discord-bot
    restart: unless-stopped
    init: true
    env_file:
      - .env
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
